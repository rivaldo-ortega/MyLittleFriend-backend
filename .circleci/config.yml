version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Instalando dependencias para llevar a produccion
          commmand: npm install
  test:
    docker:
      - image: circleci/node
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Instalando dependencias para llevar a produccion
          commmand: npm install
workflows:
  heroku_deploy:
    jobs:
      - build
      - test
